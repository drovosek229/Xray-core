- Add a second XHTTP benchmark around packet-up request shaping (`FillPacketRequest`, `ApplyMetaToRequest`, xpadding placement) so optimization work can move beyond XMUX selection without overfitting the current XMUX microbenchmark.
- Add explicit lazy-XMUX correctness tests for off-cursor request-limit and reusable-deadline expiry cases, now that off-cursor closed-client cleanup is covered.
- Re-profile after the typed-pointer snapshot and `atomic.Uint32` cursor wins. If continuing XMUX work, focus on what still dominates inside the fast scan itself (`OpenUsage.Load`, `XmuxConn.IsClosed`, atomic cursor math / modulo, atomic `sweepDue` reads) and on any remaining locked slow-path work; keep the fallback-on-closed behavior because skipping lazy cleanup broke warm-refill correctness.
- Deprioritize recently tried XMUX directions unless a fresh profile justifies them: hot-loop inlining inside the picker loops, cached request/deadline booleans, no-cleanup double-scan fast paths, direct cursor-return fast paths on the no-concurrency picker, inline warm-refill guard gating in `GetXmuxClient`, precomputed picker-mode dispatch, concurrency-local guard removal in already-specialized picker loops, warm-full/refill-gating bookkeeping, broader manager-level flag caching, optional cached closed-state helpers for `XmuxConn.IsClosed`, generic loop-counter reshuffles, lock-free variants that silently skip over scanned closed clients, folding the `sweepDue` gate into a nil snapshot sentinel, splitting the lock-free fast path into extra helper methods, and the heavier `atomic.Value` snapshot transport now superseded by the typed-pointer snapshot path.
