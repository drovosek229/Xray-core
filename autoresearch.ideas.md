- Add explicit lazy-XMUX correctness tests for off-cursor request-limit and reusable-deadline expiry cases, now that off-cursor closed-client cleanup is covered.
- Re-profile on the wider `xhttp_ns_sum` workload. Current combined profile still shows XMUX fast-path costs (`closedFlag.Load`, `OpenUsage.Load`, atomic `sweepDue` reads, specialized/general snapshot pointer loads, atomic cursor increment, and non-power-of-two cursor setup) as a large share, but request shaping remains dominant overall: `encoding/base64.(*Encoding).Encode`, `FillPacketRequest`, memmove/memclr from request-shaping allocations, remaining string concatenation, `GetRequestHeader`, `ApplyMetaToRequest`, and `net/url.Values.Encode` still show up. Prefer optimizations that help real clients, not just the benchmark harness.
- Promising request-shaping directions: look for another allocation reduction around payload chunk naming / cookie assembly without perturbing semantics, revisit `GetRequestHeader` only if a profiling-backed template/cache approach avoids the regressions from the direct textproto rewrite, and keep an eye on whether request-side `query-in-header` construction can be improved in a way that helps the full mixed workload instead of just the body-placement slice.
- Deprioritize recently tried request-shaping directions unless a fresh profile justifies them: rebuilding intermediate payload header/cookie collections, per-payload-chunk `fmt.Sprintf` key formatting, generic `io.ReadAll` on known-length bodies, recomputing the normalized chunk-size range on every chunk, routing safe metadata/xpadding cookies through `AddCookie`, routing safe non-duplicate query params through `url.Values` parse+encode, the request-side `query-in-header` req.URL/empty-RawURL rewrite, the direct textproto-based `GetRequestHeader` rewrite, the one-byte overread probe in `readRequestBody`, and the branchy fixed-range specialization that duplicated chunk/padding loops.
- Deprioritize recently tried XMUX directions unless a fresh profile justifies them: hot-loop inlining inside the picker loops, cached request/deadline booleans, no-cleanup double-scan fast paths, direct cursor-return fast paths on the no-concurrency picker (including the broadened non-power-of-two all-closed no-concurrency variant), inline warm-refill guard gating in `GetXmuxClient`, precomputed picker-mode dispatch, concurrency-local guard removal in already-specialized picker loops, warm-full/refill-gating bookkeeping, broader manager-level flag caching, broad control-flow flattening in `tryGetXmuxClientFast`, snapshot metadata bitfield packing, combined snapshot/sweep gate reshapes, separate specialized snapshot transport for the non-power-of-two + all-closed case, revisiting the dedicated smaller power-of-two/all-closed specialized snapshot transport, most generic loop-counter reshuffles outside the new masked-index cursor wins, lock-free variants that silently skip over scanned closed clients, folding the `sweepDue` gate into a nil snapshot sentinel, splitting the lock-free fast path into extra helper methods, and the heavier `atomic.Value` snapshot transport now superseded by the typed-pointer snapshot path.
